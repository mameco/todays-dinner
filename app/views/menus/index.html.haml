.wrapper
  = render partial: "header"
  .footer
    .menuList
      .listTop
        キーワード検索
        = link_to "#", class: "listTop__btn" do
          30分以内
        = link_to "#", class: "listTop__btn" do
          冷凍食品
        = link_to "#", class: "listTop__btn" do
          お惣菜
        = link_to "#", class: "listTop__btn" do
          作り置き


      .listBottom
        - @menus.each do |menu|
          = render partial: "menu", locals: { menu: menu }




